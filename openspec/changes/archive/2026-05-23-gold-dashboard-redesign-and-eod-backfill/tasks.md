## 1. Dashboard 视觉重构

- [x] 1.1 提炼 Dashboard 的新视觉 token、字体与颜色常量，统一深色金色研究风格。
- [x] 1.2 重构 `GoldForecastDashboard.vue` 的首屏结构，突出最新价格、方向、置信度与关键结论。
- [x] 1.3 将 OHLC、交易研究字段、agent 摘要、风险 notes 和免责声明重新分区，形成更清晰的信息层级。
- [x] 1.4 更新 loading、error、empty 状态的视觉与文案，使其与新 Dashboard 风格一致。
- [x] 1.5 验证移动端、平板和桌面端布局在不同宽度下均保持可读与可点击。

## 2. 收市后补数能力

- [x] 2.1 设计并实现 CSV 最新日期与缺失交易日的计算逻辑，支持识别需要补齐的 completed daily bars。
- [x] 2.2 接入 agent-assisted 数据查询流程，获取缺失日期的候选黄金 OHLC 数据并规范化输出。
- [x] 2.3 实现候选数据的确定性校验与拒绝路径，确保无效、重复或冲突数据不会写入 CSV。
- [x] 2.4 实现 CSV 原子写回逻辑，保证补数过程失败时不会损坏原始文件。
- [x] 2.5 提供一个独立的 daily backfill CLI 入口，便于调度层在美国收市后触发执行。

## 3. 测试与验证

- [x] 3.1 为缺失日期计算、候选数据校验和原子写回增加后端单元测试。
- [x] 3.2 为 Dashboard 新布局补充前端验证，确认关键信息区、状态页和响应式结构正常。
- [x] 3.3 运行后端与前端相关检查，确认变更在提交前通过基本验证。
