## 1. 信息架构与版式重组

- [x] 1.1 梳理 dashboard 当前首屏与下方区域的视觉优先级，明确 hero、图表、摘要、风险、历史和免责声明的分区顺序
- [x] 1.2 设计桌面端 12 栅格布局、平板端两列布局和移动端单列布局的页面结构草案
- [x] 1.3 定义各区域的标题层级、卡片层级和重点信息高亮规则

## 2. 视觉系统统一

- [x] 2.1 统一 dashboard 的背景、边框、阴影、圆角、渐变和状态标签样式
- [x] 2.2 收敛现有卡片风格，形成 hero card、metric card、evidence card、risk card 与 chart card 的统一视觉语法
- [x] 2.3 调整字体层级、字重、行高和字间距，使核心结论更醒目、辅助文本更克制

## 3. 页面组件重排

- [x] 3.1 重排 `GoldForecastDashboard.vue` 的首屏 summary、交易字段、chart、风险与历史模块顺序
- [x] 3.2 调整 `MarketCandlestickChart.vue` 的标题、信息密度和辅助说明位置，使其更适合作为研究证据区
- [x] 3.3 优化 loading、empty、error 和 success 的展示区块结构，减少空白和误导性占位

## 4. 响应式与可访问性

- [x] 4.1 调整 375px、768px、1024px、1440px 下的布局断点和卡片排列方式
- [x] 4.2 检查键盘焦点、对比度、hover 和 reduced motion 的视觉表现是否保留可访问性
- [x] 4.3 确保风险区、历史区和表格在窄屏下仍可阅读且不发生内容溢出

## 5. 验证与收口

- [x] 5.1 运行前端类型检查、lint 和 build，确认仅影响展示层且未破坏既有契约
- [x] 5.2 对照 OpenSpec 需求核查页面是否仅使用现有字段与现有接口
- [x] 5.3 形成视觉检查清单，确认没有改动后端接口、类型字段、路由、store 或 query key
